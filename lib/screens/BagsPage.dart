import 'package:flutter/material.dart';

class BagsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bags Page'),
        backgroundColor: Colors.blueAccent,
      ),
      body: Center(
        child: Text('This is the Bags Page'),
      ),
    );
  }
}
